{
  "name": "agentic-working-playbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "examples:index": "node scripts/generate-examples-index.mjs",
    "docs:dev": "npm run examples:index && vitepress dev docs",
    "docs:build": "npm run examples:index && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "dependencies": {
    "mermaid": "^11.15.0",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
